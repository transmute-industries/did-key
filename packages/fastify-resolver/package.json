{
  "name": "@transmute/did-key-fastify-resolver",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "test": "jest",
    "start": "node ./src/server.js",
    "docker:build": "docker build -t transmute/did-key-resolver -f ./docker/Dockerfile .",
    "docker:start": "docker rm -f $(docker ps -a -q) && docker run -p 3000:3000 --env-file ./.docker.env --name did-key-resolver -t transmute/did-key-resolver",
    "docker:stop": "docker stop did-key-resolver"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "did-method-key": "^0.2.0",
    "dotenv": "^8.2.0",
    "fastify": "^2.10.0",
    "fastify-cors": "^2.1.3",
    "fastify-swagger": "^2.4.0"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "supertest": "^4.0.2"
  }
}
